# OpenRouter Credits Widget 프로젝트 기획서

## 1. 제품 정의

OpenRouter API 키를 기기에 안전하게 저장하고, 홈 화면 위젯에서 계정의 현재 크레딧 잔액을 즉시 확인하거나 새로고침할 수 있는 Android 앱이다. 앱 본체는 OpenRouter 모바일 웹을 여는 얇은 브라우저 셸이며, 네이티브 설정 화면에서 위젯과 시작 페이지를 관리한다.

### 핵심 사용자 가치

- 앱을 열지 않고도 현재 잔액과 누적 총 충전액을 한눈에 확인한다.
- 위젯을 한 번 눌러 최신 잔액을 가져온다.
- 로고를 누르면 OpenRouter의 선택된 주요 페이지로 바로 이동한다.
- API 키, 자동 새로고침 주기, 시작 페이지를 한곳에서 관리한다.

### 지원 범위

- Android 스마트폰 및 홈 화면 위젯
- 시스템 다크/라이트 모드 자동 대응
- 단일 OpenRouter 계정/API 키
- 한국어 UI 우선, 금액은 USD 기준
- 온라인 조회만 지원하고 마지막 성공 값을 오프라인 캐시로 표시

### MVP 비지원 범위

- 여러 계정/키 프로필 전환
- 잔액 부족 알림 및 임계값 알림
- 결제 또는 크레딧 충전의 네이티브 구현
- iOS
- 사용 내역 자체 분석/차트 생성

## 2. 중요한 전제와 요구사항 조정

### 2.1 크레딧 API

- 엔드포인트: `GET https://openrouter.ai/api/v1/credits`
- 인증: `Authorization: Bearer <api-key>`
- 응답 필드: `data.total_credits`, `data.total_usage`
- 계산:
  - 충전량 = `total_credits`
  - 누적 사용량 = `total_usage`
  - 원시 잔액 = `total_credits - total_usage`
  - 표시 잔액 = `max(원시 잔액, 0)`
- `D:\DEV\PP\llm-usage-meter`의 2026-09 실사용 검증에 따르면 일반 추론용 키(`sk-or-v1-...`)로도 자기 계정의 크레딧을 조회할 수 있다. 따라서 설정에서는 **일반 OpenRouter API 키**를 기본으로 받는다.
- 403은 일반 키 전체가 지원되지 않는다는 뜻으로 간주하지 않고, 해당 키로 크레딧을 읽을 수 없는 권한 오류로 처리한다. 키를 자동 삭제하지 않고 다른 키를 사용하도록 안내한다.
- 키 생성 안내 URL은 `https://openrouter.ai/settings/keys`로 한다.

### 2.2 Google OAuth를 위한 웹 컨테이너 결정

Google OAuth 정책은 앱이 제어하는 임베디드 user-agent(WebView)로 OAuth 요청을 보내는 것을 허용하지 않는다. OpenRouter는 앱 개발자가 소유한 사이트도 아니므로 WebView의 User-Agent 위조나 세션 쿠키 복사는 구현하지 않는다.

대안 검토 결과는 다음과 같다.

- **Custom Tabs — 채택:** 기본 브라우저의 쿠키, 저장된 로그인, 자동완성, 결제 및 Google OAuth 기능을 공유하면서 앱 안에서 열린 것과 유사한 탐색 경험을 제공한다.
- **WebView — 제외:** Google OAuth 정책과 브라우저 세션 분리 때문에 필수 로그인 요구를 만족하지 못한다.
- **Trusted Web Activity — 제외:** 앱과 웹사이트가 같은 소유자임을 Digital Asset Links로 증명해야 하므로 제3자 사이트인 `openrouter.ai`에는 적용할 수 없다.
- **기본 외부 브라우저 — 폴백:** Custom Tabs 지원 브라우저가 없을 때만 사용한다.
- **네이티브 Google Credential Manager — 제외:** 앱이 받은 Google 자격 증명을 OpenRouter 세션으로 교환할 공개 연동 계약이 없으므로 OpenRouter의 자체 웹 인증을 대신할 수 없다.

MVP에서는 OpenRouter의 크레딧·로그·활동 페이지 전체를 Custom Tabs로 열며, Google 로그인 이후에도 같은 브라우저 쿠키 저장소에서 세션이 유지되어야 한다.

## 3. 화면 및 사용자 흐름

### 3.1 최초 실행

1. API 키 유무와 관계없이 선택된 시작 페이지를 Custom Tab으로 연다. 따라서 사용자는 먼저 Google 로그인하거나 OpenRouter에서 API 키를 생성할 수 있다.
2. 사용자가 Custom Tab 상단 우측 `⋮` > `앱 설정`을 선택하면 네이티브 설정 화면을 연다.
3. 일반 OpenRouter API 키를 입력하고 “확인 및 저장”을 누른다.
4. 앱이 공백·따옴표·`Authorization:`·`Bearer ` 접두사를 정리하고 `sk-or-...` 형식을 검사한다.
5. 형식이 유효하면 키를 암호화 저장한 뒤 `/api/v1/credits`를 즉시 호출한다.
6. 성공하면 위젯 갱신과 주기 작업 등록을 수행한다.
7. 401이면 거부된 저장 키를 삭제하고 재입력을 안내한다. 403이면 키는 유지하고 다른 키를 사용하도록 안내한다. 네트워크·서버 오류도 키를 유지하고 다음 주기에 재시도한다.

### 3.2 앱 본체

- 본문과 상단바: AndroidX Browser의 Custom Tab
  - `setShowTitle(true)`로 현재 OpenRouter 페이지 제목 표시
  - 시스템 다크/라이트에 맞춘 툴바 색상
  - 브라우저가 제공하는 우측 `⋮` 메뉴에 `앱 설정` 항목 추가
  - `앱 설정`의 PendingIntent는 네이티브 설정 Activity 실행
- 시작 페이지
  - 크레딧: `https://openrouter.ai/settings/credits`
  - 로그: `https://openrouter.ai/logs`
  - 활동: `https://openrouter.ai/activity`
- 뒤로가기, 팝업, 다운로드, 파일 선택, 외부 스킴, 카메라·마이크 권한은 사용자의 브라우저가 처리한다.
- Custom Tabs 미지원 환경에서는 동일 URL을 기본 브라우저로 연다.
- 앱은 OpenRouter 로그인 쿠키나 Google 자격 증명에 접근하거나 복사하지 않는다.

### 3.3 설정

- OpenRouter API 키
  - 비밀번호 형식 입력, 기본 마스킹
  - 표시/숨김 버튼
  - 붙여넣기 허용
  - “확인 및 저장”, “키 삭제”
  - 저장된 키 전문은 로그, 오류 보고, 백업에 포함하지 않음
- 자동 새로고침
  - 사용 안 함 / 15분 / 30분 / 1시간 / 3시간 / 6시간 / 12시간 / 24시간
  - 기본값: 1시간
  - Android 절전 정책 때문에 정확한 시각은 보장되지 않는다는 보조 문구
- 앱 시작 페이지
  - 크레딧(기본) / 로그 / 활동
- 위젯 미리보기 또는 “지금 새로고침” 액션
- 개인정보 안내 및 오픈소스 라이선스 진입점

## 4. 위젯 사양

### 4.1 고정 크기와 레이아웃

- 사용자 표현 `1×4`는 **세로 1칸 × 가로 4칸**, 즉 Android 메타데이터 기준 4×1로 해석한다.
- `targetCellWidth=4`, `targetCellHeight=1`, `resizeMode="none"`으로 고정하고 약 250dp×40dp를 기준으로 설계한다. 실제 외곽 크기와 패딩은 런처별로 달라질 수 있다.
- 좌측 36~40dp 고정 터치 영역: OpenRouter 로고
- 중앙의 우선 정보: 현재 잔액 `$74.75`
  - 약 24~28sp, Bold/Semibold, tabular numeral 적용
  - 위젯에서 가장 높은 대비와 가장 큰 시각적 면적 사용
- 우측의 보조 정보: `누적 충전  $100.50`
  - 약 12~14sp, Regular
  - 현재 잔액보다 작은 크기와 약 50~60% 명도/알파로 시각적 우선순위를 낮춤
  - 한 줄 유지, 우측 정렬
  - 현재 잔액과 텍스트 하단선을 맞추고 위젯 하단까지의 여백을 동일하게 유지
- 게이지, 진행 막대, 퍼센트, 차트는 표시하지 않는다.
- 한 줄 높이 제약 때문에 평상시 마지막 갱신 시각은 생략한다. 오래된 값이나 오류일 때만 작은 상태 아이콘/짧은 문구로 금액 옆에 표시한다.
- 글꼴 배율이 커지거나 금액이 길어지면 현재 잔액을 우선하고 누적 충전액의 글자 크기 축소 또는 말줄임을 먼저 적용한다.

### 4.2 터치 영역

- 로고 터치: 앱 본체를 선택된 시작 페이지로 실행
- 로고를 제외한 위젯 영역 터치: 수동 새로고침
- 중복 탭 방지: 같은 갱신 작업을 unique work로 합치고 진행 중에는 로딩 상태 표시
- 새로고침 완료 후 위젯만 갱신하며 앱을 열지 않음

### 4.3 테마

- 시스템 테마를 기본으로 자동 전환
- 라이트: 밝은 표면, 짙은 본문, 브랜드 강조색
- 다크: 어두운 표면, 밝은 본문, 명도 대비를 확보한 강조색
- Android 12 이상에서는 선택적으로 동적 색상을 사용하되 로고 원색과 정보 위계는 유지한다.
- 현재 잔액은 WCAG AA 수준의 대비를 목표로 하고, 누적 충전액도 옅게 표현하되 읽을 수 있는 최소 대비는 유지한다.

### 4.4 상태 정의

- 키 없음: `API 키 설정 필요`; 로고는 선택된 OpenRouter 페이지를 열고, 나머지 영역을 누르면 네이티브 설정 화면을 연다.
- 최초 로딩: 자리표시자/`불러오는 중…`
- 성공: 로고, 현재 잔액, 누적 총 충전액
- 네트워크 실패: 마지막 성공 값을 유지하고 `오프라인 · 12:40` 표시
- 401: 거부된 키와 해당 키의 화면 캐시를 제거하고 `API 키 설정 필요` 표시
- 403: 키와 마지막 성공 값은 유지하고 `다른 키 필요` 표시
- 서버 오류/시간 초과: 마지막 성공 값 유지, 재시도 가능
- 충전량 0: 현재 잔액과 누적 충전액 모두 `$0.00`
- 사용량이 충전량을 초과: 표시 잔액은 `$0.00`, 원본 값은 진단 로그에 남기지 않음

### 4.5 디자인 기준 시안

- 최종 승인 시안: `design/widget-concept-v2.png`
- 이전 시안: `design/widget-concept-v1.png`
- 구현 기준: 36dp 로고, 28sp Bold 현재 잔액, 14sp 옅은 누적 충전액, 36dp 세로 구분선
- 구성: 동일 레이아웃의 라이트/다크 테마 2종
- 세로 구분선과 금액 텍스트의 하단 정렬은 최종 확정 요소다.
- 시안에서 검토할 항목:
  - 로고와 현재 잔액의 상대 크기
  - 현재 잔액과 누적 충전액 사이의 간격
  - 누적 충전액의 글자 크기와 명도
  - 두 금액 텍스트의 하단 정렬과 동일한 하단 여백
  - 모서리 반경, 그림자, 테두리 강도
  - 실제 4×1 크기에서의 가독성

## 5. 기술 설계

### 5.1 권장 스택

- Kotlin
- 단일 Activity + Jetpack Compose(Material 3)
- AndroidX Browser Custom Tabs
- Jetpack Glance App Widget
- WorkManager: 자동 및 수동 백그라운드 갱신
- Retrofit 또는 Ktor Client + Kotlinx Serialization
- Preferences DataStore: 비민감 설정과 마지막 표시 데이터
- Android Keystore + AES-256-GCM: API 키 암호화
- minSdk 26 권장, target/compile SDK는 구현 시점 최신 안정 버전

### 5.2 모듈/패키지 구조

```text
app/
  data/
    remote/       OpenRouter API, DTO, 오류 매핑
    security/     Keystore 기반 키 암복호화
    preferences/  새로고침 주기, 시작 페이지, 캐시
    repository/   CreditsRepository
  domain/
    model/        CreditsSnapshot, RefreshStatus
    usecase/      RefreshCredits, ObserveSettings
  ui/
    browser/      Custom Tabs 실행, 색상, 메뉴 PendingIntent
    settings/     설정 화면
    theme/        라이트/다크 테마
  widget/
    CreditsWidget, Receiver, 상태 렌더링, 탭 액션
  worker/
    CreditsRefreshWorker, 스케줄러
```

### 5.3 데이터 모델

```text
CreditsSnapshot
- totalCredits: Decimal
- totalUsage: Decimal
- remainingCredits: Decimal
- fetchedAt: Instant
- state: Fresh | Stale | AuthError | NetworkError
```

금액 계산은 이진 부동소수점 오차를 피하기 위해 API 파싱 이후 `BigDecimal`을 사용한다. 표시는 기본 소수점 둘째 자리까지 하되 큰 값에는 로케일별 천 단위 구분자를 쓴다.

### 5.4 갱신 파이프라인

```text
위젯 탭/주기 작업/키 저장
  -> unique WorkManager 작업 요청
  -> 암호화 키 복호화
  -> HTTPS로 Credits API 호출
  -> 응답 검증 및 잔액 계산
  -> 마지막 성공 스냅샷 저장
  -> 설치된 모든 위젯 갱신
```

- `updatePeriodMillis`는 0으로 두고 WorkManager로 사용자 설정 주기를 제어한다.
- WorkManager 반복 작업의 최소 주기는 15분이며 실행은 Doze/배터리 최적화 때문에 지연될 수 있다.
- 네트워크 연결 조건을 사용한다.
- 429/5xx/일시적 네트워크 오류만 지수 백오프로 재시도한다.
- 401은 재시도하지 않고 저장 키를 삭제한다. 403은 키를 보존하되 자동 재시도하지 않고 다른 키가 필요하다고 표시한다.
- 설정 변경 시 기존 periodic unique work를 교체한다.

### 5.5 보안 및 개인정보

- API 키 암호화 키는 Android Keystore에 비추출형으로 생성한다.
- 암호문과 IV만 앱 내부 저장소에 보관하고 Android 백업에서 제외한다.
- 네트워크 보안 구성에서 평문 HTTP를 차단한다.
- API 호출 대상 호스트는 `openrouter.ai`로 고정하고 리디렉션 시 Authorization 헤더가 타 호스트로 전달되지 않게 한다.
- Custom Tab은 비임시 모드로 열어 기본 브라우저의 로그인 쿠키를 공유한다.
- 앱은 Custom Tab의 쿠키, 로컬 저장소, 페이지 DOM 또는 입력 정보에 접근하지 않는다.
- 앱 설정 메뉴 PendingIntent는 명시적 컴포넌트와 immutable 플래그를 사용한다.
- 로그에는 키, Authorization 헤더, 웹 쿠키, 전체 API 응답을 남기지 않는다.
- 스크린샷 차단은 기본 적용하지 않되 설정 화면에서 키가 노출될 때는 민감 화면 정책을 검토한다.

## 6. 오류 처리와 관측성

| 조건 | 앱 메시지 | 위젯 표시 | 재시도 |
|---|---|---|---|
| 키 없음 | OpenRouter API 키를 입력하세요 | API 키 설정 필요 | 없음 |
| 401 | 키가 거부되어 삭제되었습니다 | API 키 설정 필요 | 없음 |
| 403 | 이 키로는 크레딧을 읽을 수 없습니다 | 마지막 값 + 다른 키 필요 | 없음 |
| 429 | 요청이 너무 많습니다 | 마지막 값 + 지연 | 백오프 |
| 시간 초과/오프라인 | 네트워크를 확인하세요 | 마지막 값 + 오프라인 | 백오프 |
| 5xx | OpenRouter 응답 오류 | 마지막 값 + 오류 | 백오프 |
| 파싱 오류 | 응답 형식이 변경되었습니다 | 마지막 값 + 오류 | 제한적 |

진단 로그는 이벤트 종류, HTTP 상태 코드, 소요 시간, 갱신 출처만 기록하며 자격 증명과 금액 원문은 제외한다.

## 7. 테스트 전략과 승인 기준

### 7.1 단위 테스트

- `total_credits - total_usage` 및 0 하한 계산
- `total_credits == 0`과 사용량 초과 시 표시 잔액 0 하한
- USD 표시, 반올림, 큰 숫자
- 401/403/429/5xx/파싱 오류 매핑
- 시작 페이지 enum과 URL 매핑
- 새로고침 주기 변경 시 작업 교체
- 암호화 저장/복호화/키 손상 처리

### 7.2 통합 및 UI 테스트

- MockWebServer로 성공과 모든 주요 오류 응답 검증
- 키 저장 성공 직후 위젯 갱신
- 위젯 본문 탭은 새로고침만, 로고 탭은 앱만 실행
- 여러 위젯 인스턴스 동시 갱신
- 라이트/다크 및 글꼴 배율 1.0/1.3/2.0 스냅샷
- 4×1 고정 레이아웃, 잘림, 런처별 패딩 확인
- 로고 → 현재 잔액 → 누적 충전액 순서와 각 영역 터치 경계 확인
- 현재 잔액과 누적 충전액의 크기·굵기·명도 위계 스냅샷 확인
- 프로세스 재시작과 기기 재부팅 뒤 주기 작업 복구
- Custom Tab의 뒤로가기, 팝업, 외부 스킴, 다운로드, 파일 선택 확인
- Google 로그인 완료와 이후 OpenRouter 세션 유지 여부를 실기기에서 확인
- Custom Tab `⋮`의 `앱 설정` 항목이 네이티브 설정 화면을 여는지 확인
- Custom Tabs 미지원 브라우저에서 기본 브라우저 폴백 확인

### 7.3 MVP 완료 조건

- 유효한 일반 OpenRouter API 키로 표시 잔액이 OpenRouter API 결과와 일치한다.
- 현재 잔액은 크게, 누적 총 충전액은 작고 옅게 표시되며 게이지 요소가 존재하지 않는다.
- 위젯이 세로 1칸 × 가로 4칸 고정 크기로 제공되고 사용자가 리사이즈할 수 없다.
- 앱을 열지 않고 위젯 본문 탭으로 새로고침할 수 있다.
- 로고 탭은 네트워크 요청 대신 앱을 연다.
- 시스템 테마 변경 시 앱과 위젯이 읽기 가능한 색상으로 전환된다.
- 설정한 시작 페이지와 갱신 주기가 앱 재시작 후 유지된다.
- 키는 평문 저장, 백업, 로그 어디에도 나타나지 않는다.
- 실패 시 마지막 성공 값이 사라지지 않고 오래된 값임을 표시한다.
- Google OAuth가 Custom Tab에서 정상 완료되고 같은 브라우저 세션으로 OpenRouter 기능을 계속 사용할 수 있다.

## 8. 개발 단계

### 0단계 — 기술 검증

- 실제 일반 OpenRouter API 키로 Credits API 계약을 재확인하고 401/403 동작 기록
- OpenRouter의 크레딧/로그/활동 URL과 모바일 렌더링 확인
- Google 로그인 리디렉션, 세션 유지, `앱 설정` 메뉴를 Custom Tabs 실기기에서 검증
- 공식 OpenRouter 로고 사용 조건 및 에셋 확보

산출물: 짧은 기술 검증 기록과 OAuth 방식 확정.

### 1단계 — 프로젝트 기반

- Gradle/Compose/DI/네트워크/데이터 저장 구조 설정
- 테마, Custom Tabs 실행기/메뉴, 설정 화면 뼈대
- 단위 테스트 기반 구성

### 2단계 — 크레딧 데이터와 보안

- API 클라이언트, 오류 모델, Repository
- Keystore 암호화 저장
- 키 검증 및 마지막 성공 캐시
- 관련 단위/통합 테스트

### 3단계 — 위젯

- 고정 4×1 위젯 레이아웃과 상태 렌더링
- 로고/본문의 분리된 탭 동작
- 수동 갱신과 다중 위젯 업데이트
- 테마/크기별 UI 테스트

### 4단계 — 자동 갱신과 Custom Tabs 호환성

- WorkManager 스케줄링과 백오프
- 시작 페이지 설정 반영
- Custom Tabs 색상, 제목, 설정 메뉴와 기본 브라우저 폴백
- 실기기 OAuth 검증

### 5단계 — 출시 준비

- 접근성, 배터리, 오프라인, 복원 테스트
- 개인정보처리방침 및 Play Console Data safety 작성
- 서명 빌드, 난독화/축소, 보안 점검
- 내부 테스트 트랙 배포 후 제조사별 위젯 동작 확인

## 9. 주요 리스크와 결정 필요 사항

1. **브라우저별 Custom Tabs 기능 차이 — 중간**
   Custom Tabs를 기본으로 사용하고, 미지원 브라우저에서는 기본 브라우저로 폴백한다. OAuth와 설정 메뉴는 Chrome뿐 아니라 주요 브라우저에서 실기기 검증한다.
2. **일반 API 키의 크레딧 조회 동작 변경 가능성 — 중간**
   참조 구현에서 일반 `sk-or-v1-...` 키의 정상 조회를 2026-09에 확인했지만 공식 문서 또는 서버 정책이 바뀔 수 있다. 403이면 키를 지우지 않고 다른 API 키를 시도하도록 안내한다.
3. **OpenRouter 웹 URL/DOM 변경 — 중간**
   URL은 원격 서비스 소유이므로 출시 전 확인하고, 앱 업데이트 없이 매핑을 바꾸는 원격 설정은 MVP 이후 검토한다.
4. **백그라운드 실행 지연 — 중간**
   주기는 목표 간격이며 정확한 알람이 아니다. 수동 탭을 항상 제공하고 마지막 갱신 시각을 표시한다.
5. **상표/로고 사용 — 중간**
   OpenRouter 공식 브랜드 사용 허가와 배포 명칭을 출시 전에 확인한다. 비공식 앱임을 스토어 설명에서 명확히 한다.
6. **4×1 위젯의 제한된 높이 — 중간**
   큰 글꼴과 긴 금액에서 누적 충전액을 먼저 축소/말줄임하고 큰 현재 잔액을 보존한다. 런처가 셀 크기와 패딩을 최종 결정하므로 주요 제조사 런처에서 확인한다.

## 10. 권장 제품 결정 요약

- 앱 형식: Kotlin/Compose 네이티브 설정 화면 + OpenRouter 전체 탐색 Custom Tabs
- 위젯: Glance 고정 4×1, 로고와 본문 터치 액션 분리
- 데이터: OpenRouter Credits API, 일반 `sk-or-v1-...` 키 지원
- 표시값: 큰 현재 잔액 `max(누적 총 충전액 - 누적 총 사용액, 0)` + 작고 옅은 누적 총 충전액
- 시각 요소: 게이지·퍼센트·차트 없음
- 자동 갱신: WorkManager, 기본 1시간, 최소 15분
- 보안: Android Keystore AES-256-GCM, 백업/로그 제외
- 기본 시작 페이지: 크레딧
- 실패 정책: 마지막 성공 값을 보존하고 stale 상태를 명확히 표시
